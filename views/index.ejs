<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
  <div class="main w-full min-h-screen bg-zinc-900">
    <div class="form p-10 text-white">
      <form action="">
        <input class="block px-5 py-2 w-full rounded-md outline-none bg-zinc-800" type="text" placeholder="Title goes here"/>
        <textarea class="block px-5 py-2 w-full rounded-md outline-none bg-zinc-800 mt-2 resize-none" placeholder="Write your task details here"></textarea>
        <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md hover:bg-blue-500" type="submit" value="Create Task"/>
      </form>
    </div>
    <div class="tasks flex gap-3 flex-wrap p-10">
      <% if (files && files.length > 0) { %>
        <% files.forEach(function(file){ %>
          <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
            <h1 class="text-white text-2xl tracking-tighter"><%= file %></h1>
            <a href="" class="text-blue-400 inline-block mt-3 hover:text-blue-300">Read More</a>
          </div>
        <% }) %>
      <% } else { %>
        <h3 class="text-zinc-600">No tasks yet</h3>
      <% } %>
    </div>
  </div>
</body>
</html>